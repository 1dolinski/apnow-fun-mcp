const config = {
  port: 3000,
  secret: "",
  server: {
    name: 'apinow-fun-mcpserver',
    version: '1.0.0'
  },
  tools: {
    baseDir: './tools',
    enabled: ['apinow_search', 'apinow_execute']
  }
};

export default config;
